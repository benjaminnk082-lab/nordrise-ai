{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'echo BOOT_MARKER_AAA; ls -la /app; which bash; bash --version; bash /app/docker-entrypoint.sh'",
    "restartPolicyType": "NEVER"
  }
}
